<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="invistissement.list">
      <field name="name">invistissement list</field>
      <field name="model">invistissement.invistissement</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="invistissement.action_window">
      <field name="name">invistissement window</field>
      <field name="res_model">invistissement.invistissement</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="invistissement.action_server">
      <field name="name">invistissement server</field>
      <field name="model_id" ref="model_invistissement_invistissement"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="invistissement" id="invistissement.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="invistissement.menu_1" parent="invistissement.menu_root"/>
    <menuitem name="Menu 2" id="invistissement.menu_2" parent="invistissement.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="invistissement.menu_1_list" parent="invistissement.menu_1"
              action="invistissement.action_window"/>
    <menuitem name="Server to list" id="invistissement" parent="invistissement.menu_2"
              action="invistissement.action_server"/>
    -->
  </data>
</odoo>